package com.sparta.pages.models;

import com.sparta.pages.SearchPageModelAbstract;
import org.openqa.selenium.WebDriver;

public class SearchPageModel extends SearchPageModelAbstract {

    public SearchPageModel(WebDriver webDriver) {
        super(webDriver);
    }
}
